
  /**
   * Increases the index of the current question
   * @title Increases the index of the current question
   * @category Custom
   * @author Your_Name
   */
  const increaseQuestionIndex = async () => {
    
    // bump question index
    user.questionIndex += 1
    
  }

  return increaseQuestionIndex()